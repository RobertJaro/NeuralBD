base_dir: /gpfs/data/fs71254/schirni/NeuralBD_muram
data:
  type: 'MURAM'
  data_path: '/gpfs/data/fs71254/schirni/nstack/data/I_out_med.468000'
  n_images: 50
  pixel_per_ds: 511.5
  x_crop: 332
  y_crop: 332
  crop_size: 256
  psf_type: 'grid'

logging:
  project: NeuralBD
  name: MURAM

training:
  epochs: 10000
  log_every_n_steps: 100
  name: MURAM
model:
  dim: 512
