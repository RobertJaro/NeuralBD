base_dir: runs/spatial_numpy
work_dir: runs/spatial_numpy/work
method: spatial

data:
  type: numpy
  path: examples/data/synthetic_burst.npy
  n_images: 4
  pixel_per_ds: 32.0

model:
  image:
    dim: 64
    n_layers: 3
  psf:
    target_size: 9
    dim: 32
    n_layers: 3
    permute_samples: true

training:
  epochs: 10
  validation_every_n_epochs: 1
  checkpoint_every_n_epochs: 5
  progressive:
    sampling_points: 128

outputs:
  sample_count: 5
